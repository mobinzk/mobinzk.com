{
  "name": "mobinzk.com",
  "version": "1.0.0",
  "description": "Mobin Zadeh Kochak personal web page",
  "scripts": {
    "build": "npm install && npm run sass & npm run autoprefixer",
    "sass": "node-sass --sourcemap=none --output-style compressed -o css sass",
    "autoprefixer": "postcss -use autoprefixer --autoprefixer.browsers 'last 5 versions' -r css/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^7.2.6",
    "node-sass": "^4.14.1",
    "postcss-cli": "^8.3.1"
  }
}
